spring:
  datasource:
    url: jdbc:mysql://localhost:3306/invest?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${DB_USERNAME:root}  # 기본값 root
    password: ${DB_PASSWORD:1234}  # 기본값 1234
    driver-class-name: com.mysql.cj.jdbc.Driver   # ? ? ? ??

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
